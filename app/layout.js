export const metadata = {
  title: "접수 타이머",
  description: "고객 접수 시간 관리",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  );
}
